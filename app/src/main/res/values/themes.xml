<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FrontendEvents" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FrontendEvents" parent="Base.Theme.FrontendEvents" />
    <style name="Theme.FrontendEvents.Splash" parent="Base.Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/pink</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.FrontendEvents</item>
    </style>


</resources>